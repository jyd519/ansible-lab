---

- name: playbook for testing
  hosts: all
  become: yes
  roles:
    - { role: common, tags: ["common"] }
    - { role: nvim, tags: ["nvim"], become: yes }
    - { role: nginx, tags: ["nginx"] }
